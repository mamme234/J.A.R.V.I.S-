{
  "name": "jarvis-pro",
  "version": "2.0.0",
  "description": "JARVIS AI Assistant - Full Stack",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix backend",
    "postinstall": "npm install --prefix backend"
  },
  "engines": {
    "node": "18.x"
  }
}
